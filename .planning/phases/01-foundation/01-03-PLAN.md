---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/EPIPanel.tsx
  - frontend/src/components/VideoFeed.tsx
  - frontend/src/components/AlertCard.tsx
  - frontend/src/components/Controls.tsx
  - frontend/src/components/StatusBar.tsx
  - frontend/src/app/page.tsx
autonomous: false
requirements:
  - CONF-02

must_haves:
  truths:
    - "User can check/uncheck EPIs in a sidebar panel and changes take effect immediately on the stream"
    - "All 6 EPIs shown unchecked by default"
    - "EPI selection persists across stop/start"
    - "Stopped state shows placeholder with 'Clique em Iniciar para comecar o monitoramento'"
    - "Running state shows 'Monitorando' with green dot"
    - "Alert cards show Portuguese EPI name, timestamp, confidence %, and frame thumbnail"
    - "Alerts ordered newest first, max 50 with scroll"
  artifacts:
    - path: "frontend/src/components/EPIPanel.tsx"
      provides: "Sidebar checkbox panel for 6 EPIs"
      min_lines: 40
    - path: "frontend/src/components/VideoFeed.tsx"
      provides: "Video feed with stopped/running/error states and cache-busting"
      contains: "isRunning"
    - path: "frontend/src/components/AlertCard.tsx"
      provides: "Alert cards with Portuguese labels and badge format"
      contains: "ausente"
    - path: "frontend/src/app/page.tsx"
      provides: "Restructured layout with sidebar + video + alerts"
      contains: "EPIPanel"
    - path: "frontend/src/types/index.ts"
      provides: "Updated types for EPI config and new alert format"
      contains: "EPIItem"
    - path: "frontend/src/lib/api.ts"
      provides: "API functions for EPI config GET/POST"
      contains: "getEPIConfig"
  key_links:
    - from: "frontend/src/components/EPIPanel.tsx"
      to: "frontend/src/lib/api.ts"
      via: "EPIPanel calls getEPIConfig and updateEPIConfig"
      pattern: "getEPIConfig|updateEPIConfig"
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/components/EPIPanel.tsx"
      via: "Layout includes EPIPanel in sidebar position"
      pattern: "EPIPanel"
    - from: "frontend/src/components/VideoFeed.tsx"
      to: "frontend/src/lib/api.ts"
      via: "VideoFeed receives isRunning prop to switch between placeholder and stream"
      pattern: "isRunning"
---

<objective>
Build the frontend EPI selection panel, restructure the monitoring page layout, update VideoFeed for stopped/running states, and update alert cards for Portuguese EPI format.

Purpose: Users need the UI to select which EPIs to monitor and see the results with proper Portuguese labels and state indicators.
Output: Complete frontend with sidebar EPI panel, proper video states, and Portuguese alert cards.
</objective>

<execution_context>
@/home/badmuriss/.claude/get-shit-done/workflows/execute-plan.md
@/home/badmuriss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Backend API contracts from Plan 02 -->

GET /api/config/epis response:
```json
{
  "epis": [
    {"key": "luvas", "label": "Luvas", "active": false},
    {"key": "colete", "label": "Colete", "active": false},
    {"key": "protecao_ocular", "label": "Protecao ocular", "active": false},
    {"key": "capacete", "label": "Capacete", "active": false},
    {"key": "mascara", "label": "Mascara", "active": false},
    {"key": "calcado_seguranca", "label": "Calcado de seguranca", "active": false}
  ]
}
```

POST /api/config/epis request:
```json
{"active_epis": ["capacete", "luvas"]}
```

POST /api/config/epis response: Same as GET (updated state).

GET /api/status response:
```json
{
  "camera_active": true,
  "model_loaded": true,
  "fps": 24.8,
  "uptime": 42.3
}
```

GET /api/alerts response:
```json
{
  "alerts": [
    {
      "id": "uuid",
      "timestamp": "2026-03-05T...",
      "violation_type": "Capacete ausente",
      "confidence": 0.0,
      "frame_thumbnail": "base64..."
    }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types, API client, and build all frontend components</name>
  <files>frontend/src/types/index.ts, frontend/src/lib/api.ts, frontend/src/components/EPIPanel.tsx, frontend/src/components/VideoFeed.tsx, frontend/src/components/AlertCard.tsx, frontend/src/components/Controls.tsx, frontend/src/components/StatusBar.tsx, frontend/src/app/page.tsx</files>
  <action>
    **types/index.ts:**
    1. Remove `ViolationType` union type (no longer relevant -- alerts now use Portuguese strings).
    2. Change `Alert.violation_type` from `ViolationType` to `string`.
    3. Add `EPIItem` interface: `{ key: string; label: string; active: boolean }`.
    4. Add `EPIConfig` interface: `{ epis: EPIItem[] }`.
    5. Add `monitoring_active: boolean` to `SystemStatus` (or use existing `camera_active`).

    **lib/api.ts:**
    1. Add `getEPIConfig(): Promise<EPIConfig>` -- GET /api/config/epis.
    2. Add `updateEPIConfig(activeEpis: string[]): Promise<EPIConfig>` -- POST /api/config/epis with `{ active_epis: activeEpis }`.
    3. Add proper error handling: if response is not ok, throw with status text.

    **EPIPanel.tsx (NEW component):**
    1. "use client" component.
    2. On mount, call `getEPIConfig()` to load initial state.
    3. Render 6 checkboxes, one per EPI. Each shows `epi.label` next to a checkbox.
    4. All unchecked by default (backend returns all inactive).
    5. On checkbox toggle: immediately call `updateEPIConfig(newActiveKeys)` with the updated list of active EPI keys. Update local state optimistically.
    6. Styling: Compact vertical list with Tailwind. Each item: checkbox + label in a row. Panel has a heading "EPIs Monitorados". Use `w-56` or similar for sidebar width. Border and bg matching existing dark theme (border-gray-800, bg-gray-900/50).

    **VideoFeed.tsx:**
    1. Accept `isRunning: boolean` prop.
    2. Three states:
       - `!isRunning`: Show dark placeholder with centered text "Clique em Iniciar para comecar o monitoramento" in gray-400.
       - `isRunning && !hasError`: Show `<img>` with MJPEG stream. Append `?t=${Date.now()}` cache buster to STREAM_URL. Use a React key based on a counter to force remount on each start.
       - `isRunning && hasError`: Show error text.
    3. When `isRunning` changes from false to true, reset `hasError` to false and increment the key counter (forces img remount).

    **Controls.tsx:**
    1. Accept `isRunning: boolean`, `onStart: () => void`, `onStop: () => void` as props (lift state to page.tsx).
    2. Show "Iniciar" button when stopped, "Parar" when running. Or keep both but disable the irrelevant one.

    **StatusBar.tsx:**
    1. Change status display from "Online/Offline" to per CONTEXT.md decisions:
       - Running: Green dot + "Monitorando"
       - Stopped: Gray dot + "Parado"
    2. Keep FPS display when running.

    **AlertCard.tsx:**
    1. Remove `VIOLATION_LABELS` mapping (backend now sends Portuguese strings directly in `violation_type`).
    2. Display `alert.violation_type` directly as the label (e.g., "Capacete ausente").
    3. Add a warning-colored badge (amber/yellow bg with text). Format: EPI name as card title, with "ausente" badge.
    4. Show: EPI name (from violation_type), timestamp, confidence % (skip if 0), frame thumbnail.

    **page.tsx -- restructure layout:**
    1. Convert to "use client" (needs state for isRunning).
    2. Add state: `isRunning` (boolean), poll `/api/status` every 2 seconds to update.
    3. Layout restructure per CONTEXT.md decision (sidebar left of video):
       ```
       Header row: title + Controls(isRunning, onStart, onStop) + StatusBar
       Main grid: [EPIPanel (narrow sidebar)] [VideoFeed (wide)] [AlertPanel (medium)]
       ```
    4. Use Tailwind grid or flex: `flex` with EPIPanel at `w-56 shrink-0`, VideoFeed at `flex-1`, AlertPanel at `w-80 shrink-0`. On mobile (`< lg`), stack vertically.
    5. Pass `isRunning` to VideoFeed, Controls.
    6. `onStart`: Call `startStream()`, then poll status to confirm.
    7. `onStop`: Call `stopStream()`, then poll status to confirm.
    8. EPI selection persists across stop/start because it lives in the backend, not in frontend session state. No frontend reset needed.

    **Build check:** Run `cd frontend && npm run build` to verify no TypeScript errors.
  </action>
  <verify>
    <automated>cd /home/badmuriss/Documents/dev-projects/vigilante-ai/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend builds without errors. EPIPanel component exists with 6 checkboxes. VideoFeed shows placeholder when stopped, stream when running. AlertCard shows Portuguese labels. Layout has sidebar + video + alerts. StatusBar shows "Monitorando"/"Parado".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 1 frontend</name>
  <files>frontend/src/app/page.tsx</files>
  <action>Human verifies the complete Phase 1 frontend works end-to-end by running both backend and frontend and testing all interactive flows.</action>
  <verify>Human visual inspection of running application</verify>
  <done>User approves that EPI panel, video states, alert cards, and stop/start all work correctly.</done>
  <what-built>Complete Phase 1 frontend: EPI selection panel (sidebar with 6 checkboxes), video feed with stopped/running states and Portuguese bounding box labels, restructured monitoring layout, Portuguese alert cards with badge format, and "Monitorando"/"Parado" status indicator.</what-built>
  <how-to-verify>
    1. Start the backend: `cd backend && .venv/bin/python -m app.main`
    2. Start the frontend: `cd frontend && npm run dev`
    3. Open http://localhost:3000

    **Stopped state:**
    - Verify video area shows dark placeholder with "Clique em Iniciar para comecar o monitoramento"
    - Verify status shows "Parado" with gray dot
    - Verify EPI panel sidebar is visible with 6 unchecked EPIs

    **Running state:**
    - Check 2-3 EPIs (e.g., Capacete, Luvas)
    - Click "Iniciar"
    - Verify status changes to "Monitorando" with green dot
    - Verify video stream shows bounding boxes with Portuguese labels (e.g., "Capacete 92%") in GREEN
    - Verify only selected EPIs appear on stream (unselected EPIs not shown even if detected)
    - Verify alerts panel shows "X ausente" alerts for missing active EPIs (in Portuguese)

    **Live toggle:**
    - While running, uncheck an EPI -- it should stop appearing on stream immediately
    - Check a new EPI -- it should start appearing

    **Stop/start:**
    - Click "Parar" -- verify alerts clear, placeholder returns, EPI selections remain checked
    - Click "Iniciar" again -- verify stream restarts without crash
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` passes with no errors
- EPIPanel.tsx exists and imports getEPIConfig/updateEPIConfig
- VideoFeed.tsx accepts isRunning prop
- AlertCard.tsx no longer has VIOLATION_LABELS mapping
- page.tsx includes EPIPanel in layout
- StatusBar.tsx shows "Monitorando"/"Parado"
</verification>

<success_criteria>
- User sees 6 EPI checkboxes in a sidebar (all unchecked by default)
- Toggling checkboxes immediately affects what appears on the video stream
- Stopped state shows Portuguese placeholder message
- Running state shows "Monitorando" with green dot
- Alert cards show Portuguese format ("Capacete ausente" with badge)
- Stop/start works without crash, EPI selections persist
- Frontend builds without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
